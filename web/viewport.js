class Viewport {
	constructor(cHeight, cWidth, mHeight, mWidth, userX, userY, xMult, yMult) {
		this.canvas = { height: cHeight, width: cWidth };
		this.map = { height: mHeight, width: mWidth };
	//	this.user = { x: userX, y: userY };
	//	this.mult = { x: xMult, y: yMult };
	}
	moveViewport(){
        
    }
}

export default Viewport;